# DECISIONS — MUCH 홈페이지 리뉴얼 설계 결정

> G2(인터뷰) 산출물. 결정의 **결과**를 남기는 문서이며, 결정에 이른 사고 과정은
> [[RATIONALE]]에, 작업 진행은 [[WORKLOG]]에 각각 기록한다.
> 표기 규약은 `~/.claude/rules/doc-style.md`를 따른다 (볼트 밖 파일이라 링크하지 않는다).

## 읽는 법

- **폭발 반경**: P0은 뒤집히면 전면 재설계가 필요한 결정, P1은 페이지 구성과 작업량이
  달라지는 결정, P2는 되돌리기 쉬운 결정이다.
- 각 항목에는 **재검토 조건**을 함께 적는다. 조건이 충족되면 그 결정은 다시 열린다.

---

## P0-1. 사이트 정체성 — 과제 시리즈 허브 + MUCH 성과 아카이브

- **확정일**: 2026-09-10 (사용자 인터뷰)
- **결정**: 홈을 CHIC → MUCH → 후속 과제로 이어지는 **과제 시리즈 허브**로 두고,
  그 아래에 MUCH의 성과 아카이브를 담는다. MUCH 단독 사이트로 만들지 않는다.
- **근거**: MUCH는 2025년 12월에 종료된 과제이므로 단독 사이트는 갱신이 멈춘 채
  방치된다. 시리즈 허브로 두면 후속 과제가 시작될 때 홈만 갱신하면 되고, 종료된
  과제의 성과는 아카이브로서 계속 인용될 수 있다.
- **파생 효과**: 정보 구조가 `/`(허브) → `/much/`(아카이브) → `/projects/…`(다른 과제)
  형태로 갈린다. 후속 과제 자리를 "준비 중" 카드로 미리 확보한다.
- **재검토 조건**: 후속 과제가 별도 기관 주관으로 진행되어 같은 사이트에 담을 수 없게 되는 경우.

## P0-2. 호스팅 — 정적 사이트 + GitHub Pages

- **확정일**: 2026-09-10 (사용자 인터뷰)
- **결정**: 서버를 두지 않는 정적 사이트로 만들고 GitHub Pages로 배포한다.
  계정은 업무용 `ETRI-ULSOO`를 사용한다.
- **근거**: 종료된 과제의 아카이브에는 데이터베이스도 로그인도 필요하지 않다.
  연구소 PC가 나가는 연결만 허용하므로 `git push` 기반 배포가 방화벽과 충돌하지 않는다.
  자매 과제 CHIC이 같은 구성으로 이미 운영되고 있어 절차가 검증되어 있다 (2026-09-11 실측).
- **파생 효과**: 모든 콘텐츠가 Git 저장소 안의 텍스트 파일이 된다. 7.1 GB 발표자료와
  영상 원본은 저장소 밖에 두어야 한다 (GitHub의 파일당 상한은 100 MB다).
- **재검토 조건**: ETRI 기관 정책이 외부 호스팅을 금지하는 경우. 착수 전 사용자가 확인한다.

## P0-3. 정적 사이트 생성기 — Astro (Hugo 결정을 뒤집음)

- **확정일**: 2026-09-11 (사용자 결정)
- **개정 이력**: 2026-09-10 계획서는 Hugo를 채택했으나 하루 만에 뒤집혔다.
- **결정**: **Astro**를 사용한다. 버전과 Node 버전은 CHIC 저장소와 같은 값으로 맞춘다
  (CHIC 실측 2026-09-11: Astro 7.1.6, Node 22, `.nvmrc`로 고정).
- **뒤집은 근거** (2026-09-11 실측): 자매 과제 CHIC의 홈페이지가 이미 Astro로 완성되어
  `https://etri-ulsoo.github.io/CHIC-Homepage/`에 배포 중이며, 같은 GitHub 조직과
  같은 사용자가 두 대의 컴퓨터로 운영하고 있다. 원래 Hugo를 고른 근거 세 가지가
  모두 이 사실 앞에서 무너졌다.
  1. "Node 의존성이 두 컴퓨터 사이에서 부담이 된다" → 같은 사용자가 이미 Node 22로
     운영 중이므로 새로 지는 부담이 아니다.
  2. "Hugo의 이중언어 기능이 가장 성숙하다" → CHIC이 Astro에서 `/ko/`·`/en/` 완전
     대칭을 페이지 파일 복제 없이 구현해 두었다. 작동하는 구현이 성숙도 비교를 대체한다.
  3. "선례가 없으니 의존성이 가벼운 쪽이 낫다" → 선례가 있었다. 계획서를 쓸 때
     탐색 범위가 작업 폴더 안으로 한정되어 자매 저장소를 찾지 못했다.
- **결정적 근거**: [[DECISIONS#P0-1. 사이트 정체성 — 과제 시리즈 허브 + MUCH 성과 아카이브|P0-1]]에서
  고른 시리즈 허브 구조는 두 사이트가 같은 구조와 컴포넌트를 공유할 때에만 성립한다.
  서로 다른 생성기로 만들면 한쪽을 고칠 때마다 같은 수정을 두 벌로 해야 한다.
- **파생 효과**: 3단계(뼈대 구축)의 작업량이 크게 줄어든다. CHIC의 이중언어 라우팅,
  GitHub Pages 하위 경로 헬퍼, 배포 워크플로를 구조째 가져올 수 있다.
- **재검토 조건**: 없다. 이 결정을 다시 뒤집으려면 CHIC 사이트 자체를 옮겨야 한다.

## P0-4. 저장소 구조 — 별도 저장소 `ETRI-ULSOO/much-site`

- **확정일**: 2026-09-11 (사용자 결정)
- **결정**: MUCH 사이트를 **새 저장소** `ETRI-ULSOO/much-site`에 만든다.
  CHIC-Homepage 저장소는 건드리지 않고, 허브 홈에서 링크로만 연결한다.
- **검토한 대안과 기각 이유**:
  - *CHIC-Homepage를 시리즈 허브로 확장*: 공유 자산이 한 벌로 유지되는 이점이 있으나,
    이미 배포 중인 사이트의 주소 구조와 배포가 함께 흔들린다. CHIC에는 미해결 항목이
    여덟 건 남아 있어 (2026-09-11 확인) 그것까지 떠안게 된다.
  - *허브를 세 번째 저장소로 분리*: 경계는 가장 깨끗하나 저장소 셋을 동시에 관리하는
    부담이 현재 규모에 견주어 지나치다.
- **파생 효과**: 공개 주소는 `https://etri-ulsoo.github.io/much-site/`가 된다.
  하위 경로 때문에 내부 링크를 전부 헬퍼 함수에 통과시켜야 한다
  (CHIC의 `withBase()`·`stripBase()` 방식을 그대로 쓴다).
  CHIC과 공유하는 자산은 초기에 복사하고, 공유 대상이 실제로 늘어나면 그때 공통
  패키지로 분리한다. 쓰이지도 않을 공유 계층을 미리 만들지 않는다.
- **계정 규칙**: `git push` 전에 `gh auth status`의 활성 계정이 `ETRI-ULSOO`인지 확인한다.
  저장소의 로컬 커밋 이메일은 `hkkim79@etri.re.kr`로 지정했다 (2026-09-11 설정 완료).
- **재검토 조건**: 후속 과제 사이트를 만들 때 공유 자산이 세 벌로 늘어나면,
  그 시점에 공통 패키지 분리를 다시 검토한다.

## P1-1. 1차 독자 — 박물관 실무자와 일반 대중을 함께

- **확정일**: 2026-09-10 (사용자 인터뷰)
- **결정**: 박물관·문화유산 기관의 실무자와, 일반 대중·언론을 **함께** 1차 독자로 삼는다.
- **섞는 방식**: 홈을 "보여주기 → 쓰기" 순서의 두 층으로 쌓는다.
  1. 위층(대중·언론): 히어로 영상, 시범 콘텐츠 갤러리, 수상, 언론 보도 띠,
     그리고 숫자 넉 줄(논문·특허·데이터셋·표준).
  2. 아래층(실무자): "박물관 실무자라면" 카드로 플랫폼이 해 주는 일, 표준·가이드라인
     내려받기, 문의로 잇는다.
  3. 맨 아래: 과제 시리즈 타임라인.
- **근거**: 두 독자가 사이트에서 찾는 것이 다르지만 서로 배타적이지 않다. 대중은
  결과물을 보고 싶어 하고 실무자는 그 결과물을 자기 기관에 어떻게 적용할지 알고 싶어 한다.
  같은 콘텐츠를 순서로 갈라 놓으면 한 페이지로 두 요구를 모두 받을 수 있다.
- **파생 효과**: 성과 지표표와 논문 목록 같은 심사용 콘텐츠는 `/much/outcomes/`에 두고,
  홈에는 숫자 넉 줄만 띠로 보여 준다.
- **재검토 조건**: 접속 기록을 확인할 수 있게 된 뒤, 한쪽 독자가 압도적이면 층 구성을 조정한다.

## P1-2. 디자인 — 사용자가 직접 만들어 넘기고, 구조만 CHIC에서 물려받는다

- **확정일**: 2026-09-11 (사용자 결정). 같은 날 개정: 처음에는 "MUCH 고유 디자인을
  새로 잡는다"로 정했고, 곧이어 **그 디자인을 사용자가 직접 만들어 넘기는** 것으로 구체화했다.
- **결정**: 시각 디자인은 **사용자가 직접 수행해 Claude에게 넘긴다.** Claude는 넘겨받은
  결과물을 토큰과 컴포넌트로 옮겨 구현한다. CHIC에서는 **구조와 배관만** 물려받는다.

| 계승한다 (CHIC) | 사용자가 정한다 | Claude가 만든다 |
|---|---|---|
| Astro 프로젝트 구성과 `[locale]` 이중언어 라우팅 | 색·서체·간격 | 넘겨받은 디자인의 토큰화 |
| 하위 경로 헬퍼 (`withBase`·`stripBase`) | 홈과 페이지의 짜임새 | 컴포넌트 구현과 반응형 처리 |
| GitHub Actions 배포 워크플로 | 컴포넌트의 외형 | [[DESIGN]] 문서 정리 |
| 콘텐츠와 데이터를 가르는 방식 (본문은 Markdown, 표는 YAML) | | |
| 컴포넌트의 **구조**와 받는 속성 (영상 갤러리·수치 표시·도해 액자) | | |
| 대용량 비공개 자료를 저장소에서 제외하는 규칙 | | |

- **근거**: 디자인은 "보면 아는" 기준이 지배하는 영역이고, 그 기준을 가진 쪽이
  사용자다. 시안을 만들어 고르게 하는 절차보다 직접 만들어 넘기는 편이 빠르고 정확하다.
- **파생 효과**:
  - 계획의 **2단계 성격이 바뀐다.** Claude가 서로 다른 계열 네 방향을 목업으로
    제시하는 작업(G3)은 수행하지 않는다. 대신 넘겨받은 결과물을 읽어 토큰으로
    정리하고, 빠진 값(호버 상태, 오류 색, 모바일 간격 등)만 목록으로 되물어 채운다.
  - **2단계가 1단계와 3단계를 막지 않는다.** 원고 정리(1단계)와 뼈대 구축(3단계)은
    디자인이 오기 전에도 진행할 수 있으므로, 디자인 대기 시간에 작업을 멈추지 않는다.
  - CHIC의 `tokens.css`는 **참고 자료일 뿐 복사 대상이 아니다.**
- **넘겨받을 형태**: 어떤 형태든 받는다. 다만 정보량이 많은 쪽일수록 옮길 때 어긋남이
  적다. HTML과 CSS가 가장 정확하고, 그다음이 Figma 파일과 색·서체 값이 적힌 시안이며,
  그림 파일만 있는 경우에는 값을 눈으로 재야 해서 확인 질문이 늘어난다.
- **계승하되 검증할 것**: CHIC의 디자인 문서에 적힌 접근성 규칙 세 가지는 색이 바뀌어도
  유효하므로 넘겨받은 디자인에서도 확인하고, 어긋나면 보고한다. ① 본문 대비비 4.5:1 이상,
  ② 한글에 대문자 변환과 넓은 자간을 쓰지 않기, ③ 고채도 악센트 색을 밝은 배경의
  본문 글자에 쓰지 않기.
- **재검토 조건**: 없다. 결과물을 받는 시점만 미정이다.

---

## P1-3. 디자인 시안 — 시안 나(단청)를 채택한다

**결정일**: 2026-09-11. **결정자**: 사용자.

**내용**: 사용자가 `design/` 폴더로 넘겨준 시안 두 벌 가운데 **시안 나(단청 계열, 어두운 바탕)**를
사이트의 바탕으로 삼는다. 확정 토큰은 [[DESIGN]] 3절이며, 채택하지 않은 시안 가(한지 계열,
밝은 바탕)의 값은 같은 문서 2절에 근거 추적용으로 남겼다.

**근거 두 가지**

- **영상과의 이음새**: 사이트에 싣기로 한 영상 열아홉 편이 대부분 어두운 전시 공간에서
  촬영되었고, 공공장소 대형 화면 콘텐츠도 검은 배경에 유물이 떠 있는 형태다. 어두운 바탕은
  영상과 지면의 경계를 드러내지 않는다. 밝은 바탕을 고르면 영상마다 테두리 처리를 따로
  정해야 했다.
- **색을 고칠 곳이 없다**: 대비비 열다섯 쌍을 실측한 결과(2026-09-11), 시안 나는 라벨색까지
  본문 기준 4.5 대 1을 통과했다. 시안 가는 라벨색이 2.43으로 크게 미달해서 채택하면 색을
  바꿔야 했다.

**채택하면서 함께 정한 것**: 시안의 구분선 두 가지(`#23383a` 1.44, `#4a6360` 2.74)는 비텍스트
대비 3 대 1에 못 미치므로, 누를 수 있는 요소의 테두리에는 같은 색조를 밝힌 `#647f7c`
(바탕 4.11 실측)를 `--line-ui`로 새로 두었다. 적 `#c3452f`와 청 `#3f7d72`는 글자에 쓰지 않고
큰 제목과 도형에만 쓴다.

**재검토 조건**: 없다. 다만 시안에 없는 구성 요소 세 가지(숫자 띠, 언어 전환, 연표)는
3단계에서 만들어야 하므로, 그때 시안의 인상과 어긋나면 [[DESIGN]] 4절을 고친다.

---

## 미결 항목

| 번호 | 내용 | 막고 있는 단계 |
|---|---|---|
| Q-1 | ETRI 기관의 외부 사이트 운영 정책 확인 (저장소 공개 전환 전 필요) | 6단계 배포 |
| Q-2 | 논문 11편·학술대회 발표 18건·국내 특허 6건·소프트웨어 7건의 서지 정보 원본. 최종보고서 78쪽의 별첨 5~7에 해당하지만 제공받은 PDF에 별첨이 들어 있지 않다 (2026-09-11 확인) | 1단계 `/much/outcomes/` |
| Q-3 | 시연 영상의 게시 계정. CHIC은 개인 계정에 올려 두어 위험 항목으로 남아 있다 (2026-09-11 확인). MUCH는 처음부터 기관 채널을 쓸지 정해야 한다 | 5단계 영상 |
| Q-4 | 언론 보도 20여 건의 기사 주소와 보도 일자 | 1단계 `/much/media/` |
| Q-5 | 후속 과제의 이름·기간·소개 문구 | 1단계 홈 타임라인 |
| ~~Q-6~~ | ~~사용자가 만든 디자인 결과물과 그 형태~~ **해소 (2026-09-11)** — 시안 두 벌을 `design/` 폴더로 받았고, 시안 나를 채택해 P1-3으로 기록했다 | — |
