# DESIGN.md — 디자인 토큰과 구성 규칙

> 작성 2026-09-11. 등급 `[L2]` 2단계 산출물 (G3 수렴 결과).
> 원천: 사용자가 `design/` 폴더에 넣어 준 시안 두 벌 (2026-09-11 제공, 같은 날 실측).
> 시안은 `<x-dc>` 런타임(`design/support.js`)으로 미리 보는 형식이며, `{{ }}` 자리 표시자와
> `<sc-if>`·`<sc-for>` 같은 전용 태그를 씁니다. 이 문서는 그 시안에서 **값만 뽑아낸 것**이고,
> 실제 구현은 3단계에서 Astro 컴포넌트와 CSS 사용자 지정 속성으로 옮깁니다.

## 0. 먼저 정할 것

시안이 두 벌이고 색 계열이 정반대입니다. 어느 쪽을 바탕으로 삼을지 정해야 나머지 작업이
한 방향으로 진행됩니다. 두 벌의 값은 아래에 모두 정리해 두었으므로, 고르는 즉시 3단계로
넘어갈 수 있습니다.

| | 시안 가 (한지) | 시안 나 (단청, 파일 이름에 v2) |
|---|---|---|
| 파일 | `design/문화유산연구팀.dc.html` | `design/문화유산연구팀 v2 단청.dc.html` |
| 바탕 | 밝음 (#f2eee4 한지색) | 어두움 (#0e1a1c 먹색) |
| 인상 | 종이에 먹이 번진 고문헌 | 단청을 입힌 목조 건축의 밤 |
| 강조색 | 짙은 청록 한 가지 (#2f4a44) | 금빛·주홍·청록 세 가지 |
| 본문 서체 | Gowun Batang | Noto Serif KR |
| 제목 서체 | Nanum Myeongjo | Song Myung |
| 사진이 필요한 정도 | 높음 (여백이 사진을 기다림) | 낮음 (색 자체가 화면을 채움) |

**판단 재료 하나**: MUCH가 가진 영상 열아홉 편은 대부분 어두운 전시 공간에서 촬영되었고,
공공장소 대형 화면 콘텐츠도 검은 배경에 유물이 떠 있는 형태입니다. 시안 나의 어두운 바탕은
이 영상들과 이어 붙였을 때 경계가 드러나지 않습니다. 시안 가는 영상 둘레에 밝은 여백이
생기므로 영상마다 테두리 처리를 따로 정해야 합니다.

## 1. 두 시안이 공유하는 규칙

색과 서체는 다르지만 짜임새는 같습니다. 아래 일곱 가지는 어느 쪽을 고르든 그대로 갑니다.

1. **모서리를 둥글리지 않습니다.** 두 시안 모두 `border-radius`가 한 번도 쓰이지 않았습니다
   (2026-09-11 실측). 카드와 단추, 이미지 모두 각진 사각형입니다.
2. **테두리는 1픽셀 실선 하나로만 긋습니다.** 그림자를 쓰지 않고 선으로 영역을 나눕니다.
3. **본문 줄 간격이 넓습니다.** `line-height`가 1.8에서 2.1 사이에 몰려 있습니다. 한국어
   세리프 서체를 길게 읽히도록 잡은 값입니다.
4. **작은 라벨은 고정폭 서체에 자간을 크게 벌려 씁니다.** `IBM Plex Mono`를 9.5픽셀에서
   13픽셀 사이로 쓰고 `letter-spacing`을 .12em에서 .24em까지 줍니다. 절 번호("01 — 연구 소개"),
   영문 부제("SELECTED PUBLICATIONS"), 연도 표기가 여기 해당합니다.
5. **본문 너비를 1200픽셀 안팎으로 묶습니다.** 시안 가는 1200픽셀, 시안 나는 1240픽셀입니다.
6. **글 덩어리는 `ch` 단위로 폭을 제한합니다.** 30ch에서 56ch 사이를 씁니다. 픽셀이 아니라
   글자 수로 묶었기 때문에 서체 크기가 바뀌어도 한 줄에 담기는 글자 수가 유지됩니다.
7. **절 사이 여백이 100픽셀 안팎입니다.** 시안 가는 `100px 30px`, 시안 나는 `104px 32px`입니다.

### 화면에 나타나는 순서

두 시안의 구성이 동일합니다. 위에서부터 고정 머리말, 큰 제목 화면, 소개, 네 갈래 분야,
성과 목록, 아카이브 갤러리, 연구진, 꼬리말입니다. 이 뼈대를 MUCH 사이트에 어떻게 옮길지는
아래 4절에 적었습니다.

## 2. 시안 가 — 한지 계열 토큰

```css
:root {
  /* 바탕과 면 */
  --bg:           #f2eee4;  /* 한지색 바탕 */
  --surface:      #ece7d9;  /* 카드처럼 한 단 올라온 면 */
  --line:         #d5cdb8;  /* 기본 구분선 */
  --line-strong:  #c3b898;  /* 강조 구분선 */
  --line-faint:   #ded8c8;  /* 흐린 구분선 */

  /* 글자 */
  --ink:          #1a1a17;  /* 본문과 제목 */
  --ink-2:        #4d4a41;  /* 부연 설명 */
  --ink-3:        #5f5949;  /* 더 약한 부연 */
  --ink-muted:    #a09a87;  /* 라벨과 보조 정보 */

  /* 강조 */
  --accent:       #2f4a44;  /* 짙은 청록. 링크와 단추 */

  /* 서체 */
  --font-body:    'Gowun Batang', serif;
  --font-title:   'Nanum Myeongjo', serif;
  --font-mono:    'IBM Plex Mono', monospace;

  /* 치수 */
  --page-max:     1200px;
  --section-pad:  100px 30px;
  --header-pad:   15px 30px;
}
```

- 큰 제목 크기: `clamp(36px, 5vw, 64px)`
- 절 제목 크기: 29px에서 34px 사이
- 본문 크기: 15px에서 18px 사이

## 3. 시안 나 — 단청 계열 토큰

```css
:root {
  /* 바탕과 면 */
  --bg:           #0e1a1c;  /* 먹색 바탕 */
  --surface:      #14211f;  /* 한 단 올라온 면 */
  --line:         #23383a;  /* 기본 구분선 */
  --line-strong:  #4a6360;  /* 강조 구분선 */

  /* 글자 */
  --ink:          #f3ece0;  /* 본문과 제목 */
  --ink-2:        #d6cfc1;  /* 부연 설명 */
  --ink-3:        #c3bdb0;  /* 더 약한 부연 */
  --ink-muted:    #a79f90;  /* 라벨과 보조 정보 */

  /* 강조 — 오방색에서 가져온 세 가지 */
  --accent:       #e0b25a;  /* 황. 링크와 단추 */
  --accent-red:   #c3452f;  /* 적. 강조 표시 */
  --accent-red-2: #e06a4f;  /* 밝은 적. 상태 표시 */
  --accent-green: #3f7d72;  /* 청. 분류 표시 */

  /* 서체 */
  --font-body:    'Noto Serif KR', serif;
  --font-title:   'Song Myung', serif;
  --font-mono:    'IBM Plex Mono', monospace;

  /* 치수 */
  --page-max:     1240px;
  --section-pad:  104px 32px;
  --header-pad:   16px 32px;
}
```

- 큰 제목 크기: `clamp(40px, 6.4vw, 86px)`
- 절 제목 크기: `clamp(26px, 3vw, 36px)`
- 본문 크기: 15px에서 19px 사이

**색의 유래**: 바탕 #0e1a1c(흑), 글자 #f3ece0(백), 강조 #e0b25a(황)·#c3452f(적)·#3f7d72(청)이
전통 단청의 오방색과 그대로 대응합니다. 색을 늘리거나 바꿀 때 이 다섯 갈래를 벗어나지 않으면
시안의 인상이 유지됩니다.

## 4. 시안의 구성을 MUCH 사이트에 옮기는 방법

시안은 연구팀 소개 사이트를 상정하고 만들어졌습니다. MUCH 사이트의 구성([[PLAN]] 3절)과는
쪽 이름이 다르므로 아래와 같이 짝을 지었습니다.

| 시안의 절 | MUCH 사이트에서 대응하는 자리 |
|---|---|
| 큰 제목 화면 | 홈 히어로. 시안의 문구 자리에 "문화유산을 디지털로 잇는 공유 플랫폼"이 들어갑니다. |
| 소개 | 홈의 한 문단 요약과 `/much/` 개요 |
| 네 갈래의 기록 | 홈의 "박물관 실무자라면" 갈림길 카드 네 장 |
| 성과 목록 | `/much/outcomes/`의 논문·특허 목록. 시안의 연도 구분 방식을 그대로 씁니다. |
| 아카이브 갤러리 | `/much/showcase/`의 시범 콘텐츠. 시안은 사진을 상정했으나 MUCH는 영상이 주력이므로 5절을 따릅니다. |
| 연구진 | `/much/consortium/`의 참여 기관. 사람이 아니라 기관을 싣습니다. |
| 꼬리말 | 전 쪽 공통 꼬리말 |

시안에 없어서 새로 만들어야 하는 구성 요소는 세 가지입니다.

1. **숫자 띠** — 홈에 다섯 개 숫자를 나란히 놓는 띠입니다. 고정폭 서체로 숫자를 크게 쓰고
   그 아래에 한국어 라벨을 답니다.
2. **언어 전환** — 머리말 오른쪽에 한국어와 영어를 오가는 단추가 필요합니다. 시안에는
   없습니다.
3. **연표** — 홈 아래쪽의 과제 시리즈 타임라인입니다. 시안의 성과 목록이 쓰는 연도 구분
   방식을 가로로 눕혀 재사용합니다.

## 5. 영상을 싣는 규칙

MUCH 사이트에서 영상이 차지하는 비중이 시안보다 훨씬 큽니다. 열아홉 편을 배치하기로 했으므로
([[CONTENT-CHECK]] 3절) 아래 규칙을 디자인 층에서 미리 고정합니다.

1. **가로세로 비율을 고정하지 않습니다.** 세로형 영상이 두 편 있습니다 (media15는 1392×3914,
   media17은 1080×1920). 감싸는 상자가 영상의 비율을 따라가게 합니다.
2. **누르기 전에는 유튜브를 불러오지 않습니다.** 현행 사이트는 한 쪽에서 iframe 열한 개를
   한꺼번에 불러옵니다 ([[SITE-ANALYSIS]] 실측). 대표 이미지와 재생 단추만 먼저 그리고,
   방문자가 누른 뒤에 iframe을 만듭니다.
3. **대표 이미지가 검은 영상은 첫 장면을 쓰지 않습니다.** media2·5·8·14는 첫 장면이
   검은색이라 무엇을 담은 영상인지 알 수 없습니다. 중간 장면을 골라 씁니다.
4. **홈 배경 영상은 소리 없이 반복 재생하고, 움직임을 줄이도록 설정한 방문자에게는
   정지 이미지를 보여 줍니다.** 두 시안 모두 `[data-nomotion]` 아래에서 움직임을 끄는
   장치를 이미 갖추고 있으므로 같은 방식을 씁니다.

## 6. 접근성 기준

1. 본문 글자와 바탕의 밝기 대비를 4.5 대 1 이상으로 유지합니다. 아래 실측표를 따릅니다.
2. 모든 그림과 영상에 대체 텍스트를 답니다. 원고에 이미 적어 두었습니다.
3. 움직임을 줄이도록 설정한 방문자에게는 나타나는 동작을 모두 끕니다
   (`prefers-reduced-motion`).
4. 375픽셀 폭에서 가로로 넘치는 요소가 없어야 합니다. 시안은 `overflow-x:hidden`으로
   가리고 있으나, 구현에서는 가리지 않고 넘치지 않게 만듭니다.

### 대비비 실측 (2026-09-11 측정)

두 시안이 정한 색을 그대로 대입해 계산했습니다. 상대 휘도는 각 색 성분을 선형화한 뒤
`0.2126R + 0.7152G + 0.0722B`로 구했고, 대비비는 `(밝은 쪽 휘도 + 0.05) / (어두운 쪽 휘도 + 0.05)`입니다.
판정 기준은 본문 4.5 대 1, 큰 글자(24픽셀 이상 또는 굵은 19픽셀 이상) 3 대 1입니다.

| 시안 | 조합 | 대비비 | 판정 |
| --- | --- | --- | --- |
| 가 | 본문 `#1a1a17` / 바탕 `#f2eee4` | 15.05 | 통과 |
| 가 | 부연 `#4d4a41` / 바탕 `#f2eee4` | 7.64 | 통과 |
| 가 | 부연2 `#5f5949` / 바탕 `#f2eee4` | 6.02 | 통과 |
| 가 | 라벨 `#a09a87` / 바탕 `#f2eee4` | **2.43** | **미달** |
| 가 | 강조 `#2f4a44` / 바탕 `#f2eee4` | 8.30 | 통과 |
| 가 | 본문 `#1a1a17` / 면 `#ece7d9` | 14.12 | 통과 |
| 나 | 본문 `#f3ece0` / 바탕 `#0e1a1c` | 15.12 | 통과 |
| 나 | 부연 `#d6cfc1` / 바탕 `#0e1a1c` | 11.46 | 통과 |
| 나 | 부연2 `#c3bdb0` / 바탕 `#0e1a1c` | 9.49 | 통과 |
| 나 | 라벨 `#a79f90` / 바탕 `#0e1a1c` | 6.77 | 통과 |
| 나 | 금 `#e0b25a` / 바탕 `#0e1a1c` | 9.03 | 통과 |
| 나 | 적 `#c3452f` / 바탕 `#0e1a1c` | **3.56** | 큰 글자와 도형만 |
| 나 | 밝은 적 `#e06a4f` / 바탕 `#0e1a1c` | 5.36 | 통과 |
| 나 | 청 `#3f7d72` / 바탕 `#0e1a1c` | **3.71** | 큰 글자와 도형만 |
| 나 | 본문 `#f3ece0` / 면 `#14211f` | 14.12 | 통과 |

측정에서 따라 나온 규칙은 다음 세 가지입니다.

1. **시안 가를 채택하면 라벨색을 고쳐야 합니다.** `#a09a87`은 2.43으로 기준에 크게 못 미칩니다.
   라벨은 작은 글자에 자간을 넓혀 쓰는 자리라 오히려 읽기 어려운 조합이므로, 큰 글자 예외를
   적용하지 않고 본문 기준을 지켜야 합니다. 밝기를 낮춘 `#6f6a58`(4.67 실측) 언저리가 대안입니다.
   시안이 라벨에 의도한 옅은 인상은 색이 아니라 자간과 크기로 만듭니다.
2. **시안 나의 적과 청은 글자에 쓰지 않습니다.** 두 색 모두 3 대 1은 넘고 4.5 대 1에는 못 미치므로,
   큰 제목이나 구분선, 도형에만 쓰고 본문·라벨·설명에는 쓰지 않습니다. 붉은 글자가 필요하면
   밝은 적 `#e06a4f`(5.36)를 씁니다.
3. **어두운 시안이 대비 면에서는 여유가 큽니다.** 시안 나는 라벨색까지 본문 기준을 통과했습니다.
   0절의 판단 재료에 이 사실을 함께 놓습니다.

## 7. 하지 않을 것

- 모서리를 둥글리지 않습니다.
- 그림자를 쓰지 않습니다. 선으로만 나눕니다.
- 현행 사이트의 템플릿 색(Material Indigo 계열)을 쓰지 않습니다.
- 강조색을 시안에 없는 색으로 늘리지 않습니다.
- 본문에 고정폭 서체를 쓰지 않습니다. 라벨과 숫자에만 씁니다.

## 8. 시안에 들어 있는 예시 문구를 사실로 옮기지 않습니다

시안에는 자리를 채우기 위한 예시 문구가 들어 있습니다. 아래 항목은 MUCH의 사실이 아니므로
구현할 때 걷어냅니다.

| 시안의 문구 | 처리 |
|---|---|
| `heritage-lab@example.re.kr` | 예시 주소입니다. 실제 연락처는 [[CONTENT-CHECK]] 미결 7번을 정한 뒤에 넣습니다. |
| "EST. 2009", "SINCE 2009" | 근거가 확인되지 않은 연도입니다. MUCH 과제 기간은 2023년 4월부터 2025년 12월까지입니다. |
| "CC BY 4.0 · 출처 표기 후 자유 인용" | 자료의 이용 조건을 사용자가 확정하기 전에는 싣지 않습니다. |
| "기록되지 않은 기술은 한 세대 안에 사라진다. — 팀 설립 제안서, 2009" | 출처가 확인되지 않은 인용문입니다. |
| "현장 실측 사진 (세로 3:4)" | 사진 자리 표시자입니다. |

## 9. 미해결

| 항목 | 상태 |
|---|---|
| 두 시안 가운데 무엇을 바탕으로 삼을지 | **사용자 결정 대기** (0절) |
| 시안의 연구진 절을 참여 기관으로 바꿔 쓸지, 아니면 빼고 갈지 | 사용자 결정 대기 |
| 구글 폰트를 외부에서 불러올지, 파일을 저장소에 담을지 | 기관 정책(Q-1)과 함께 확인 |
| 세로형 영상 두 편을 갤러리에서 어떻게 배치할지 | 3단계에서 시안을 만들어 확인 |
